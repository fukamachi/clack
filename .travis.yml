language: common-lisp

sudo: false

env:
  global:
    - PATH=~/.roswell/bin:~/nginx/sbin:$PATH
    - LD_LIBRARY_PATH=$HOME/libuv/lib:$LD_LIBRARY_PATH
    - INCLUDE=$HOME/libuv/include:$INCLUDE
    - ROSWELL_INSTALL_DIR=$HOME/.roswell
    - ROSWELL_REQUIRE_SUDO=0
    - COVERAGE_EXCLUDE=t:v1-compat
  matrix:
    - LISP=sbcl COVERALLS=true
    - LISP=ccl

addons:
  apt:
    packages:
      - libfcgi-dev

before_install:
  # Install Roswell
  - curl -L https://raw.githubusercontent.com/fukamachi/roswell/require-sudo/scripts/install-for-ci.sh | sh
  # libuv for Wookie
  - curl -L https://github.com/libuv/libuv/archive/v1.2.0.tar.gz | tar xzf -
  - (cd libuv-1.2.0 && ./autogen.sh && ./configure --prefix=$HOME/libuv && make && make install)
  # nginx
  - curl -L http://nginx.org/download/nginx-1.8.0.tar.gz | tar xzf -
  - (cd nginx-1.8.0 && ./configure --prefix=$HOME/nginx && make && make install)

install:
  - ros install prove

before_script:
  - nginx -c "$TRAVIS_BUILD_DIR/t/nginx.conf" -p "$HOME/nginx"
  - ros run -- --version

script:
  - test_files=$(ls t-*.asd)
  - if [ "$LISP" != "sbcl" ]; then
      test_files=$(echo $test_files | grep -v t-clack-handler-fcgi.asd);
    fi
  - run-prove $test_files